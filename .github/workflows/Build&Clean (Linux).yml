name: Build&Clean (Linux)

on: [push]

jobs:
  build-n-clean_linux_job:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Запуск скрипта для проверки сборки
      run: |
        bash .github/scripts/Build\&Clean\ \(Linux\).sh
