name: Build&Clean (macOS)

on: push

jobs:
  Build&Clean_macOS:

    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - name: Установка и проверка gmake
      run: |
        bash .github/scripts/Build\&Clean\ \(macOS\,\ установка\ и\ проверка\ gmake\).sh
